---
- name: Ensure homelab services root exists
  ansible.builtin.file:
    path: "{{ homelab_services_root }}"
    state: directory
    mode: "0755"

- name: Ensure homelab data root exists
  ansible.builtin.file:
    path: "{{ homelab_data_root }}"
    state: directory
    mode: "0755"
