[defaults]
inventory = ansible/inventories/home.yml
roles_path = ansible/roles
host_key_checking = False

[ssh_connection]
# Multiplexing leaves stale BECOME-SUCCESS on WSL SSH, so disable it.
ssh_args = -C -o ControlMaster=no -o ControlPersist=no
pipelining = True
